import { Button } from 'components/Button';

import type { FC } from 'react';

const Main: FC = () => {
  return (
    <>
      <Button>
        Button
      </Button>
      Main pages
    </>
  );
};

export default Main;
